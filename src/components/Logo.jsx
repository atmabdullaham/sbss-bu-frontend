import logo from "../assets/logo.svg";
const Logo = () => {
  // Brand colors from application theme

  return (
    <div className="ml-0 pl-0">
      <img src={logo} alt="" />
    </div>
  );
};

export default Logo;
